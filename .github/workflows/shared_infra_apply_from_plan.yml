name: Shared Infra Apply From Plan

on:
  workflow_call:
    inputs:
      environment:
        description: environment reference i.e. 'prod' or 'dev'
        required: true
        type: string
      plan_artifact_run_id:
        description: "Workflow run ID used to recover metadata and plan artifacts"
        required: true
        type: string

permissions:
  id-token: write
  contents: read
  actions: read

env:
  AWS_OIDC_ROLE_ARN: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/${{ vars.PROJECT_NAME }}-${{ inputs.environment }}-github-oidc-role
  ARTIFACT_ENVIRONMENT: ${{ inputs.environment == 'dev' && 'dev' || 'ci' }}
  ARTIFACT_AWS_OIDC_ROLE_ARN: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/${{ vars.PROJECT_NAME }}-${{ inputs.environment == 'dev' && 'dev' || 'ci' }}-github-oidc-role

jobs:
  artifact_bucket:
    runs-on: ubuntu-latest
    outputs:
      code_bucket_name: ${{ steps.get_bucket_name.outputs.bucket }}
    steps:
      - uses: actions/checkout@v6

      - name: Get shared code bucket outputs
        uses: ./.github/actions/terragrunt
        id: code_action
        with:
          aws_oidc_role_arn: ${{ env.ARTIFACT_AWS_OIDC_ROLE_ARN }}
          tg_directory: infra/live/${{ env.ARTIFACT_ENVIRONMENT }}/aws/code_bucket
          tg_action: init

      - name: Get bucket name
        id: get_bucket_name
        env:
          TG_OUTPUTS: ${{ steps.code_action.outputs.tg_outputs }}
        run: |
          echo "bucket=$(echo "$TG_OUTPUTS" | jq -r '.bucket.value // empty')" >> "$GITHUB_OUTPUT"

  metadata:
    needs: artifact_bucket
    runs-on: ubuntu-latest
    outputs:
      infra_version: ${{ steps.read_metadata.outputs.infra_version }}
      code_bucket: ${{ steps.read_metadata.outputs.code_bucket }}
      lambda_matrix: ${{ steps.read_metadata.outputs.lambda_matrix }}
      bootstrap_image_uri: ${{ steps.read_metadata.outputs.bootstrap_image_uri }}
      service_matrix: ${{ steps.read_metadata.outputs.service_matrix }}
    steps:
      - uses: actions/checkout@v6

      - name: Download plan metadata from S3
        uses: ./.github/actions/just
        env:
          BUCKET_NAME: ${{ needs.artifact_bucket.outputs.code_bucket_name }}
          ENVIRONMENT: ${{ inputs.environment }}
          RUN_ID: ${{ inputs.plan_artifact_run_id }}
        with:
          aws_oidc_role_arn: ${{ env.ARTIFACT_AWS_OIDC_ROLE_ARN }}
          justfile_path: justfile.ci
          just_action: infra-plan-metadata-download

      - name: Check plan metadata artifact
        shell: bash
        run: |
          if [ ! -f plan-metadata.json ]; then
            echo "::error title=Missing plan metadata artifact::Expected 'plan-metadata.json' at s3://${{ needs.artifact_bucket.outputs.code_bucket_name }}/terragrunt_plan/${{ inputs.environment }}/${{ inputs.plan_artifact_run_id }}/infra-plan-metadata/plan-metadata.json, but it was not downloaded."
            exit 1
          fi

      - name: Read plan metadata
        id: read_metadata
        shell: bash
        run: |
          echo "infra_version=$(jq -r '.infra_version' plan-metadata.json)" >> "$GITHUB_OUTPUT"
          echo "code_bucket=$(jq -r '.code_bucket' plan-metadata.json)" >> "$GITHUB_OUTPUT"
          echo "lambda_matrix=$(jq -c '.lambda_matrix' plan-metadata.json)" >> "$GITHUB_OUTPUT"
          echo "bootstrap_image_uri=$(jq -r '.bootstrap_image_uri' plan-metadata.json)" >> "$GITHUB_OUTPUT"
          echo "service_matrix=$(jq -c '.service_matrix' plan-metadata.json)" >> "$GITHUB_OUTPUT"

  infra:
    needs:
      - metadata
    uses: ./.github/workflows/shared_infra.yml
    with:
      environment: ${{ inputs.environment }}
      infra_version: ${{ needs.metadata.outputs.infra_version }}
      code_bucket: ${{ needs.metadata.outputs.code_bucket }}
      lambda_matrix: ${{ needs.metadata.outputs.lambda_matrix }}
      bootstrap_image_uri: ${{ needs.metadata.outputs.bootstrap_image_uri }}
      service_matrix: ${{ needs.metadata.outputs.service_matrix }}
      tg_action: apply_plan
      plan_artifact_run_id: ${{ inputs.plan_artifact_run_id }}
